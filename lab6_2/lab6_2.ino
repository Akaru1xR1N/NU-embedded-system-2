int LED_0 = 6; // กำหนดให้ตัวแปร LED_0 = 6
int LED_1 = 7; // กำหนดให้ตัวแปร LED_1 = 7
int LED_2 = 8; // กำหนดให้ตัวแปร LED_2 = 8
int LED_3 = 9; // กำหนดให้ตัวแปร LED_3 = 9
int LED_4 = 10; // กำหนดให้ตัวแปร LED_4 = 10
int LED_5 = 11; // กำหนดให้ตัวแปร LED_5 = 11
int LED_6 = 12; // กำหนดให้ตัวแปร LED_6 = 12
int LED_7 = 13; // กำหนดให้ตัวแปร LED_7 = 13

void setup() {
  pinMode(LED_0, OUTPUT); // กำหนด port 6 เป็น output
  pinMode(LED_1, OUTPUT); // กำหนด port 7 เป็น output
  pinMode(LED_2, OUTPUT); // กำหนด port 8 เป็น output
  pinMode(LED_3, OUTPUT); // กำหนด port 9 เป็น output
  pinMode(LED_4, OUTPUT); // กำหนด port 10 เป็น output
  pinMode(LED_5, OUTPUT); // กำหนด port 11 เป็น output
  pinMode(LED_6, OUTPUT); // กำหนด port 12 เป็น output
  pinMode(LED_7, OUTPUT); // กำหนด port 13 เป็น output
}

void loop() {
  digitalWrite(LED_0, HIGH); // กำหนด port 6 เป็น HIGH
  digitalWrite(LED_1, LOW); // กำหนด port 7 เป็น LOW
  digitalWrite(LED_2, LOW); // กำหนด port 8 เป็น LOW
  digitalWrite(LED_3, LOW); // กำหนด port 9 เป็น LOW
  digitalWrite(LED_4, LOW); // กำหนด port 10 เป็น LOW
  digitalWrite(LED_5, LOW); // กำหนด port 11 เป็น LOW
  digitalWrite(LED_6, LOW); // กำหนด port 12 เป็น LOW
  digitalWrite(LED_7, LOW); // กำหนด port 13 เป็น LOW
  delay(1000); // delay 1 second

  
  digitalWrite(LED_0, LOW); // กำหนด port 6 เป็น LOW
  digitalWrite(LED_1, HIGH); // กำหนด port 7 เป็น HIGH
  digitalWrite(LED_2, LOW); // กำหนด port 8 เป็น LOW
  digitalWrite(LED_3, LOW); // กำหนด port 9 เป็น LOW
  digitalWrite(LED_4, LOW); // กำหนด port 10 เป็น LOW
  digitalWrite(LED_5, LOW); // กำหนด port 11 เป็น LOW
  digitalWrite(LED_6, LOW); // กำหนด port 12 เป็น LOW
  digitalWrite(LED_7, LOW); // กำหนด port 13 เป็น LOW
  delay(1000); // delay 1 second

  digitalWrite(LED_0, LOW); // กำหนด port 6 เป็น LOW
  digitalWrite(LED_1, LOW); // กำหนด port 7 เป็น LOW
  digitalWrite(LED_2, HIGH); // กำหนด port 8 เป็น HIGH
  digitalWrite(LED_3, LOW); // กำหนด port 9 เป็น LOW
  digitalWrite(LED_4, LOW); // กำหนด port 10 เป็น LOW
  digitalWrite(LED_5, LOW); // กำหนด port 11 เป็น LOW
  digitalWrite(LED_6, LOW); // กำหนด port 12 เป็น LOW
  digitalWrite(LED_7, LOW); // กำหนด port 13 เป็น LOW
  delay(1000); // delay 1 second

  digitalWrite(LED_0, LOW); // กำหนด port 6 เป็น LOW
  digitalWrite(LED_1, LOW); // กำหนด port 7 เป็น LOW
  digitalWrite(LED_2, LOW); // กำหนด port 8 เป็น LOW
  digitalWrite(LED_3, HIGH); // กำหนด port 9 เป็น HIGH
  digitalWrite(LED_4, LOW); // กำหนด port 10 เป็น LOW
  digitalWrite(LED_5, LOW); // กำหนด port 11 เป็น LOW
  digitalWrite(LED_6, LOW); // กำหนด port 12 เป็น LOW
  digitalWrite(LED_7, LOW); // กำหนด port 13 เป็น LOW
  delay(1000); // delay 1 second

  digitalWrite(LED_0, LOW); // กำหนด port 6 เป็น LOW
  digitalWrite(LED_1, LOW); // กำหนด port 7 เป็น LOW
  digitalWrite(LED_2, LOW); // กำหนด port 8 เป็น LOW
  digitalWrite(LED_3, LOW); // กำหนด port 9 เป็น LOW
  digitalWrite(LED_4, HIGH); // กำหนด port 10 เป็น HIGH
  digitalWrite(LED_5, LOW); // กำหนด port 11 เป็น LOW
  digitalWrite(LED_6, LOW); // กำหนด port 12 เป็น LOW
  digitalWrite(LED_7, LOW); // กำหนด port 13 เป็น LOW
  delay(1000); // delay 1 second

  digitalWrite(LED_0, LOW); // กำหนด port 6 เป็น LOW
  digitalWrite(LED_1, LOW); // กำหนด port 7 เป็น LOW
  digitalWrite(LED_2, LOW); // กำหนด port 8 เป็น LOW
  digitalWrite(LED_3, LOW); // กำหนด port 9 เป็น LOW
  digitalWrite(LED_4, LOW); // กำหนด port 10 เป็น LOW
  digitalWrite(LED_5, HIGH); // กำหนด port 11 เป็น HIGH
  digitalWrite(LED_6, LOW); // กำหนด port 12 เป็น LOW
  digitalWrite(LED_7, LOW); // กำหนด port 13 เป็น LOW
  delay(1000); // delay 1 second

  digitalWrite(LED_0, LOW); // กำหนด port 6 เป็น LOW
  digitalWrite(LED_1, LOW); // กำหนด port 7 เป็น LOW
  digitalWrite(LED_2, LOW); // กำหนด port 8 เป็น LOW
  digitalWrite(LED_3, LOW); // กำหนด port 9 เป็น LOW
  digitalWrite(LED_4, LOW); // กำหนด port 10 เป็น LOW
  digitalWrite(LED_5, LOW); // กำหนด port 11 เป็น LOW
  digitalWrite(LED_6, HIGH); // กำหนด port 12 เป็น HIGH
  digitalWrite(LED_7, LOW); // กำหนด port 13 เป็น LOW
  delay(1000); // delay 1 second

  digitalWrite(LED_0, LOW); // กำหนด port 6 เป็น LOW
  digitalWrite(LED_1, LOW); // กำหนด port 7 เป็น LOW
  digitalWrite(LED_2, LOW); // กำหนด port 8 เป็น LOW
  digitalWrite(LED_3, LOW); // กำหนด port 9 เป็น LOW
  digitalWrite(LED_4, LOW); // กำหนด port 10 เป็น LOW
  digitalWrite(LED_5, LOW); // กำหนด port 11 เป็น LOW
  digitalWrite(LED_6, LOW); // กำหนด port 12 เป็น LOW
  digitalWrite(LED_7, HIGH); // กำหนด port 13 เป็น HIGH
  delay(1000); // delay 1 second

}










